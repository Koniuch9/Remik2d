using System.Collections;
using UnityEngine;

public class PopupBehaviour : MonoBehaviour
{
    void OnEnable() {
        // LeanTween.alpha(gameObject.GetComponent<RectTransform>(), 0.2f, 0.5f);
    }
}
